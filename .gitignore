node_modules/
dist/
.env
.env.local
.env.*.local
*.log
.DS_Store
.vscode/
.idea/
coverage/
frontend/node_modules/
frontend/dist/
prisma/migrations/dev.db*

# Incremental-build metadata from tsc / vite
*.tsbuildinfo

# Bridged sibling projects (symlinks into ~/Documents/Claude Code/{New T4T,Palisade}).
# Browseable in-place for cross-project context (SUN spec, FIDO2 applet,
# personalisation flow) but not Vera's source — never commit.
external/
